#include <stdio.h>

int main() {
    //freopen("output.txt", "w", stdout);
    //freopen("input.txt", "r", stdin);
    int n;
    scanf("%d", &n);
    int sum=((1+n)*n)/2;
    printf("%d", sum);
    //first
    return 0;
}